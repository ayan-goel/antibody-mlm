# GENERATED by scripts/make_seed_configs.py — do not edit by hand.
#
# Seed replicate of /usr/scratch/agoel320/antibody-mlm/configs/germline_medium.yaml
# Differs from the base config in exactly two fields:
#   seed:                1 (was 42)
#   training.output_dir: models/checkpoints/germline_medium_s1
#
# data.data_split_seed is deliberately inherited (default 42) so this replicate
# trains and evaluates on the identical held-out split as every other run.
# Varying `seed` varies weight init and masking RNG only.

seed: 1
data:
  processed_path: data/processed/oas_vh_500k.jsonl
  max_length: 160
  min_length: 80
  train_split: 0.9
  valid_amino_acids: ACDEFGHIKLMNPQRSTVWY
  germline_path: data/structures/oas_vh_500k_germline.pt
masking:
  strategy: germline
  mask_prob: 0.15
  mask_token_ratio: 0.8
  random_token_ratio: 0.1
  params:
    mutated_weight: 6.0
    germline_weight: 1.0
model:
  model_name: alchemab/antiberta2
  from_pretrained: false
  model_size: medium
training:
  output_dir: models/checkpoints/germline_medium_s1
  batch_size: 32
  learning_rate: 5.0e-05
  num_epochs: 20
  max_steps: 125000
  warmup_steps: 6250
  weight_decay: 0.01
  logging_steps: 100
  save_steps: 2000
  eval_steps: 5000
  fp16: true
  dataloader_num_workers: 4
  gradient_accumulation_steps: 2
  early_stopping_patience: 0
